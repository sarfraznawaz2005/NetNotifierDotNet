<Window x:Class="NetNotifier.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetNotifier Settings"
        Height="330" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Test URLs (comma separated):" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="TestUrlsBox" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Check Interval (seconds):" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="IntervalBox" HorizontalAlignment="Left" Width="100" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="4" Text="HTTP Timeout (ms):" Margin="0,0,0,4"/>
        <TextBox Grid.Row="5" x:Name="TimeoutBox" HorizontalAlignment="Left" Width="100" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="6" Margin="0,0,0,12">
            <CheckBox x:Name="VoiceAlertsCheckBox" Content="Enable voice alerts" Margin="0,0,0,6"/>
            <CheckBox x:Name="StartWithWindowsCheckBox" Content="Start with Windows"/>
        </StackPanel>

        <UniformGrid Grid.Row="8" Rows="1" Columns="3">
            <Button Content="Save" Height="28" Margin="0,0,8,0" Click="SaveButton_Click" IsDefault="True"/>
            <Button Content="Test" Height="28" Margin="0,0,8,0" Click="TestButton_Click"/>
            <Button Content="Cancel" Height="28" Click="CancelButton_Click" IsCancel="True"/>
        </UniformGrid>
    </Grid>
</Window>
